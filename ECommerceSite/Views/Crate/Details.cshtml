@model ECommerceSite.Models.Crate

@{
    ViewData["Title"] = $"Details for {Model.Title}";
}

<h1>Details for @Model.Title</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CrateId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CrateId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Price)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Price)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@Model.CrateId">Edit</a>
    <a asp-action="Index">Back to List</a>
</div>
